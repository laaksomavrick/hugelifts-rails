# frozen_string_literal: true

require 'rails_helper'

RSpec.describe 'Exercises', type: :request do
  describe 'GET /index' do
    pending 'redirects to login page if user is not logged in'
    pending 'shows a users exercises' do
    end
  end
end
