<%= render 'shared/page_header', header: t('workout_days.header'), back_button: true %>

<%= render 'shared/page_subheader', subheader: t('common.details') %>

<%= render 'workout_days/form',
           model: @form,
           method: :post,
           url: workout_workout_days_path %>